import React from "react"
import Layout from "../../components/layout"
const productPostTemplate = ({location}) => (
  <Layout location={location}>
    <h1>Product Post Template</h1>
  </Layout>
)
export default productPostTemplate