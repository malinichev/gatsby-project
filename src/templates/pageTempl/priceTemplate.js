import React from "react"
import Layout from "../../components/layout"
const PriceTemplate = ({location}) => (
  <Layout location={location}>
    <h1>priceTemplate Template</h1>
  </Layout>
)
export default PriceTemplate