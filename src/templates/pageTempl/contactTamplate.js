import React from "react"
import Layout from "../../components/layout"
const ContactTamplate = ({location}) => (
  <Layout location={location}>
    <h1>ContactTamplate.js Template</h1>
  </Layout>
)
export default ContactTamplate